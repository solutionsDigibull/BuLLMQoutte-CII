services:
  app:
    build: .
    ports:
      - "8001:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:DigiBull@host.docker.internal:5433/bullmquote
      SECRET_KEY: changeme-in-production
    extra_hosts:
      - "host.docker.internal:host-gateway"
